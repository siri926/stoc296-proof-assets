# STOC-296 proof screenshots

Sanitized captures from GitHub Actions run **30427624501** (Android emulator + iOS
simulator) at SHA `e618e54cfa1944a4e84e6b5e6f5e5789efd495a8`.

Public **only** so GitHub can render them inline in the pull-request comments on
`toloka-partners/a-vc-stock-visor#2376`, which is a private repository — GitHub's
image proxy cannot fetch private content, so an anonymously reachable URL is the
only way to embed them.

All PNG metadata chunks are stripped; the image data itself is unmodified. No
usernames, filesystem paths, emails, tokens, device identifiers, browser chrome or
personal information appear in these images. No source code is present in this
repository. Delete it once the PR is reviewed.
